# 汽配云CRM工作台 — 新用户上手指南

## 快速开始（3步上手）

### 第1步：Fork 本仓库

1. 点击右上角 **Fork** 按钮
2. 选择你自己的 GitHub 账号作为目标
3. 等待 fork 完成（几秒钟）

### 第2步：获取 GitHub Token

1. 访问 [GitHub Settings → Tokens](https://github.com/settings/tokens)
2. 点击 **Generate new token (classic)**
3. 填写信息：
   - **Note**: `汽配云CRM`
   - **Expiration**: 自定义，建议 90 天
   - **勾选权限**: `repo`（仓库读写权限）
4. 点击底部 **Generate token**
5. ⚠️ **立即复制 Token**（只显示一次！）

### 第3步：登录使用

1. 打开你的 GitHub Pages 地址：
   ```
   https://你的用户名.github.io/qipeiyun-crm/
   ```
2. 粘贴刚才复制的 Token，点击登录
3. 首次登录会显示空工作台，你可以：
   - **手动添加客户**：点击"新增客户"
   - **导入备份数据**：在设置中粘贴备份码恢复数据
   - **使用通用模板**：如果你有模板数据

---

## 多用户协作说明

本工作台基于 **Fork + Token** 方式实现多用户隔离：

| 项目 | 说明 |
|------|------|
| 数据存储 | 存储在你自己 fork 的 `gh-pages` 分支 `data/` 目录下 |
| 数据格式 | JSON 文件，按 GitHub 用户名命名（如 `data/你的用户名.json`） |
| 数据同步 | 每次修改自动保存到 GitHub（2秒防抖） |
| 数据安全 | 仅你自己（持有 Token）可读写你的数据 |

---

## 常见问题

**Q: 忘记 Token 怎么办？**
A: 重新生成一个新的 Token，旧 Token 会自动失效。

**Q: 数据能分享给别人吗？**
A: 可以导出备份码，在设置中生成，分享给其他人导入。

**Q: Token 过期了怎么办？**
A: 重新生成新 Token，用新 Token 重新登录即可，数据不会丢失。

**Q: 能同时多设备使用吗？**
A: 可以，同一 Token 在多设备登录，数据自动云端同步。

---

## 独立版（离线使用）

如果需要离线使用或分享给非技术用户，可以使用**独立版**：

- 独立版内置所有数据，不需要 GitHub 账号
- 数据保存在浏览器 localStorage 中
- 可以通过备份码导出/导入数据

在设置中点击"生成独立版"即可下载。
